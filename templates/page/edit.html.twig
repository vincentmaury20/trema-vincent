{% extends 'base.html.twig' %}

{% block title %}Modifier Page{% endblock %}

{% block body %}
    <h1>Modifier la Page</h1>

    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.content) }}
        <button class="btn">Enregistrer</button>
    {{ form_end(form) }}

{% endblock %}
